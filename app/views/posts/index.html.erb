<main>
  <section>
    <div class="card">
      <div class="user-image border"></div>
      <div class="user-data border">
        <h1><%= @user.Name %></h1>
        <p class="number-posts">Number of posts: <%= @user.PostCounter %></p>
      </div>
    </div>
  </section>
  <section>
    <ul>
      <% @posts.each do |post| %>
        <%= link_to user_post_path(user_id: post.author_id, id: post.id) do %>
          <li class="card hover column">
            <div class="card column">
              <h2><%= post.Title %></h2>
              <p><%= post.Text %></p>
              <div class="right">
                <span>Comments: <%= post.CommentsCounter %></span>
                <span>Likes: <%= post.LikesCounter %> </span>
              </div>
            </div>
            <div class="commets-container">
              <h4>Comments:</h4>
              <ul>
                <% post.recent_comments.each do |comment| %>
                  <p><%= comment.author.Name %>: <%= comment.Text %></p>
                <% end %>
              </ul>
            </div>
          </li>
        <% end %>
      <% end %>
    </ul>
  </section>
</main>
